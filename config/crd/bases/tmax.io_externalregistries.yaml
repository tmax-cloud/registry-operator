
---
apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.3.0
  creationTimestamp: null
  name: externalregistries.tmax.io
spec:
  additionalPrinterColumns:
  - JSONPath: .spec.registryUrl
    name: REGISTRY_URL
    type: string
  - JSONPath: .spec.imagePullSecret
    name: IMAGE_PULL_SECRET
    type: string
  group: tmax.io
  names:
    kind: ExternalRegistry
    listKind: ExternalRegistryList
    plural: externalregistries
    shortNames:
    - exreg
    singular: externalregistry
  scope: Namespaced
  subresources:
    status: {}
  validation:
    openAPIV3Schema:
      description: ExternalRegistry is the Schema for the externalregistries API
      properties:
        apiVersion:
          description: 'APIVersion defines the versioned schema of this representation
            of an object. Servers should convert recognized schemas to the latest
            internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
          type: string
        kind:
          description: 'Kind is a string value representing the REST resource this
            object represents. Servers may infer this from the endpoint the client
            submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
          type: string
        metadata:
          type: object
        spec:
          description: ExternalRegistrySpec defines the desired state of ExternalRegistry
          properties:
            certificateSecret:
              description: Certificate secret name for private registry. Secret's
                data key must be 'ca.crt' or 'tls.crt'.
              type: string
            imagePullSecret:
              description: Login id and password secret object for registry
              type: string
            insecure:
              description: Do not verify tls certificates
              type: boolean
            registryType:
              description: Registry type like Harbor
              enum:
              - HarborV2
              type: string
            registryUrl:
              description: 'Registry URL (example: docker.io)'
              type: string
            syncPeriod:
              description: Sync period
              type: integer
          required:
          - imagePullSecret
          - registryType
          - registryUrl
          type: object
        status:
          description: ExternalRegistryStatus defines the observed state of ExternalRegistry
          type: object
      type: object
  version: v1
  versions:
  - name: v1
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
